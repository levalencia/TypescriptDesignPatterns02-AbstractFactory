/// <reference types="mocha" />

import { assert } from 'chai';

describe('TypescriptDesignPatterns02AbstractFactoryWebPart', () => {
  it('should do something', () => {
    assert.ok(true);
  });
});
